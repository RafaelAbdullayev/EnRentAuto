-- Как показывать фон первого экрана.
-- cover — заполнить экран, обрезав лишнее (подходит фотографиям);
-- contain — вписать целиком, не обрезая (нужно эмблемам и логотипам:
-- на узком экране телефона от них иначе виден только фрагмент).
ALTER TABLE "Settings" ADD COLUMN "heroFit" TEXT NOT NULL DEFAULT 'cover';
